apiVersion: v1
kind: Service
metadata:
  name: aj-blogs
  namespace: hugo
spec:
  selector:
    app: aj-blogs
  ports:
    apiVersion: v1
kind: Service
metadata:
  name: whoami
  namespace: default
spec:
  selector:
    app: whoami
  ports:
    - name: http
      port: 80
      targetPort: 80
